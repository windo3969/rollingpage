# CLAUDE.md — 롤링페이퍼 서비스

이 문서는 프로젝트의 기획, 원칙, 기술 스택, 개발 순서를 정리한 기준 문서다.
작업 전 항상 이 문서를 먼저 읽고, 여기 적힌 원칙과 충돌하는 구현은 하지 않는다.

---

## 1. 서비스 개요

**한 줄 정의**: 검색에 절대 노출되지 않는, 관리하기 쉬운, 소장할 수 있는 생일 롤링페이퍼 웹 서비스

**포지셔닝**
> 불안한 링크 대신 안전하게, 흩어지는 메시지 대신 관리되게, 스쳐가는 이벤트 대신 남는 결과물로.

**출발점이 된 문제**
- 기존 롤링페이퍼 서비스에서 작성한 롤링페이퍼가 제목만으로 구글 검색에 전부 노출됨 (이름, 개인 메시지 유출)
- 기존 서비스들의 디자인 완성도 부족
- 주최자가 누가 썼고 누가 안 썼는지 파악할 방법이 없음

**1차 타겟**: 생일 (캐주얼, 바이럴 중심)
- 주최자(생일자의 지인)가 방을 만들고 → 친구들에게 공유 → 각자 메시지 작성 → 생일자에게 전달
- 결혼/돌잔치, 학교/직장은 이후 확장 대상

---

## 2. 기능 정의

### MVP — 무료
| 기능 | 설명 |
|---|---|
| 방 생성 | 제목, 대상자 이름, 마감일, 비밀번호(선택) |
| 보안 기본값 | 랜덤 URL, 검색엔진 차단, 사진 비공개 저장 (3장 참고) |
| 로그인 없는 작성 | 링크만으로 텍스트 + 사진 1장 작성 |
| 카톡 공유 최적화 | Kakao SDK 공유 버튼, OG 태그(제목/미리보기 이미지) |
| 글감 힌트 | 관계 기반 질문형 프롬프트 (예: "이 사람과 있었던 재밌는 에피소드는?"). AI 생성 문장 아님 |
| 자동 레이아웃 | 글자 수에 따라 카드 크기 자동 조정, 결과물이 항상 정돈되어 보이게 |
| 주최자 대시보드 | 참여자 명단 기반 작성/미작성 현황, 미작성자 리마인드 |

### MVP — 유료
| 기능 | 설명 |
|---|---|
| 포토북 PDF | 사진 + 메시지를 자동 조합해 인쇄 가능한 포토북 PDF 생성 (1회 결제, 3~5천 원 수준 검토) |
| 프리미엄 템플릿 | 추가 디자인 템플릿 |

### 명시적으로 제외한 것 (MVP 범위 밖)
- 생일 당일 자동 공개/카운트다운 — 필요성 낮음, 드롭
- 터치 드로잉(손글씨) — 공수 대비 실사용률 낮음, 드롭
- AI 완성 문장 추천 — 진정성 훼손 반감 우려. 도입하더라도 "글이 안 써져요?" 버튼 뒤에 숨긴 키워드/시작 문장 힌트 수준으로만
- 회원 히스토리, 템플릿 재사용 등 재방문 기능 — MVP 이후 확장
- 카카오 알림톡 자동 발송 — 비용 발생, 초기엔 "독촉 메시지 복사" 방식
- 실물 포토북 인쇄 대행 — 이후 확장

---

## 3. 보안 원칙 (절대 규칙)

보안은 이 서비스의 핵심 차별점이다. 나중에 붙이는 기능이 아니라 첫날부터의 기본값이다.

1. **검색엔진 차단 3중 적용**
   - 모든 페이지에 `<meta name="robots" content="noindex, nofollow">`
   - 모든 응답에 `X-Robots-Tag: noindex, nofollow` 헤더
   - `robots.txt`에서 방/작성/결과 경로 전체 Disallow
   - 랜딩 페이지 등 마케팅용 공개 페이지만 예외로 허용
2. **추측 불가능한 URL**: 방 ID는 순번이 아닌 nanoid 등 랜덤 문자열 (최소 16자 이상)
3. **사진 비공개 저장**: Supabase Storage 비공개 버킷 사용, 조회 시에만 유효기간 있는 signed URL 발급. 공개 버킷 사용 금지
4. **DB 접근 제어**: Supabase RLS로 권한 규칙을 DB 레벨에서 강제. 클라이언트 코드의 체크에만 의존하지 않는다
5. **비밀번호 옵션**: 방 비밀번호는 해시 저장, 평문 저장 금지
6. **사이트맵에 방 페이지 포함 금지**
7. **신뢰 표시**: UI에 "이 페이지는 검색엔진에 노출되지 않습니다" 안내 노출 (마케팅 포인트)

---

## 4. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | 화면과 API를 한 프로젝트에서 |
| 스타일 | Tailwind CSS | |
| DB / 스토리지 | Supabase (PostgreSQL, Storage, RLS) | |
| 배포 | Vercel | |
| ID 생성 | nanoid | |
| 이미지 압축 | 브라우저에서 업로드 전 압축 (browser-image-compression 등) | 저장/트래픽 비용 절감 |
| 카톡 공유 | Kakao JavaScript SDK | |
| 결제 | 토스페이먼츠 | Phase 3 |
| PDF 생성 | Puppeteer/Playwright 기반 별도 워커 서버 (Railway, Fly.io 등) | Phase 3, Vercel 서버리스에서 돌리지 않음 |
| 분석 | PostHog 또는 GA4 | PDF 버튼 수요 측정 |

**환경 변수** (`.env.local`, 절대 커밋하지 않음)
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY` (서버 전용, 클라이언트 노출 금지)
- `NEXT_PUBLIC_KAKAO_JS_KEY`

---

## 5. 데이터 모델 (초안)

```
rooms
  id              text  PK (nanoid)
  title           text
  recipient_name  text
  deadline        timestamptz
  password_hash   text  nullable
  host_token      text  (주최자 대시보드 접근용 비밀 토큰)
  created_at      timestamptz

participants          -- Phase 2
  id              uuid  PK
  room_id         text  FK → rooms.id
  name            text
  has_written     boolean

messages
  id              uuid  PK
  room_id         text  FK → rooms.id
  participant_id  uuid  nullable FK → participants.id
  author_name     text
  content         text
  photo_path      text  nullable (비공개 버킷 내 경로)
  created_at      timestamptz
```

**참여자 추적 방식 (Phase 2)**: 주최자가 이름 명단을 입력 → 참여자는 공용 링크로 들어와 자기 이름을 선택 후 작성. 개인별 링크 발급 방식은 채택하지 않음 (공유가 번거로움).

---

## 6. 개발 순서

### Phase 1 — 핵심 흐름 + 보안 (2~3주)
목표: "만들기 → 공유 → 작성 → 받기" 흐름 완성, 지인 생일에 실사용 검증

1. Next.js + TypeScript + Tailwind 세팅, Supabase 연결
2. rooms / messages 테이블 + RLS 정책 SQL
3. 검색엔진 차단 3중 적용 (메타태그, 헤더, robots.txt)
4. 방 생성 페이지
5. 로그인 없는 메시지 작성 페이지 (사진 압축 → 비공개 버킷)
6. 결과 페이지 (글자 수 기반 자동 레이아웃, 기본 템플릿 1~2개)
7. 카톡 공유 버튼 + OG 태그
8. "포토북 PDF 받기(준비 중)" 버튼 + 클릭 이벤트 기록 (수요 검증용)
9. Vercel 배포

### Phase 2 — 차별화: 주최자 대시보드 (2주)
1. participants 테이블 + 명단 입력
2. 작성 시 명단에서 이름 선택
3. 대시보드: 작성/미작성 현황 (host_token으로만 접근)
4. 미작성자 대상 "독촉 메시지 복사" 버튼
5. 글감 힌트 (관계 기반 질문형 프롬프트)

### Phase 3 — 수익화 (2~3주)
진행 전 Phase 1의 PDF 버튼 클릭 데이터로 수요 재확인
1. 토스페이먼츠 결제 연동
2. PDF 워커 서버 (Puppeteer) 구축
3. 결제 완료 → PDF 생성 → 저장 → 다운로드 링크 흐름
4. 프리미엄 템플릿

### 이후 확장 후보
- 회원 로그인, 롤링페이퍼 히스토리, 템플릿 재사용
- 결혼/돌잔치, 학교/직장 타겟 확장
- 실물 포토북 인쇄 대행
- 카카오 알림톡 자동 리마인더

---

## 7. 작업 규칙

- 한 번에 한 단계씩 구현한다. 요청받지 않은 다음 단계 기능을 미리 만들지 않는다
- 기능 구현과 디자인 다듬기는 분리한다. 기능이 먼저 동작하게 만든 뒤 디자인 요청을 따로 받는다
- 모든 UI는 모바일 우선으로 만든다 (카톡 링크로 모바일 유입이 대부분)
- 새 페이지를 추가할 때마다 3장의 보안 원칙(noindex, RLS, signed URL)을 지켰는지 확인한다
- 비밀 키는 코드에 하드코딩하지 않고 환경 변수로만 다룬다
- 각 단계가 끝나면 무엇을 만들었고 어떻게 확인하면 되는지 짧게 정리한다
